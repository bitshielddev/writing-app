---
engines:
  metric:
    exclude_paths:
      - "**/*.test.ts"
      - "**/*.test.tsx"
exclude_paths:
  - "docs/**"
  - "artifacts/**"
  # Codacy's SQL analyzer expects T-SQL and rejects valid SQLite syntax such as
  # PRAGMA, STRICT tables, and AUTOINCREMENT.
  - "desktop/fixtures/**/*.sql"
